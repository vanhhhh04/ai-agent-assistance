# ── FastAPI runtime ────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-dotenv==1.0.1

# ── LLM gateway (provider adapters) ────────────────────
# All SDKs are installed so users can switch via LLM_PROVIDER env without rebuild.
anthropic==0.40.0
google-genai==0.8.0             # Google Gen AI SDK — Gemini 2.5 Flash/Pro
openai==1.99.0                  # OpenAI SDK — GPT-5*, GPT-4*, o-series (reasoning_effort supported)

# ── Backends ───────────────────────────────────────────
asyncpg==0.29.0                 # postgres bronze
pyhive==0.7.0                   # hive gold (HiveServer2 thrift) — base, no [hive] extra
thrift==0.16.0                  # required by pyhive
# HiveServer2 in our compose stack runs with default auth=NONE (SASL/PLAIN),
# matching what beeline / Spark expect. We pair pyhive with HIVE_AUTH=NONE.
# `pure-sasl` is the pure-Python SASL impl (no C ext → no Python-3.11 build break
# like the old `sasl` package). thrift-sasl wraps it for thrift transport.
pure-sasl==0.6.2
thrift-sasl==0.4.3

# ── Semantic layer (OpenSearch retrieval) ──────────────
opensearch-py==2.6.0
sentence-transformers==2.7.0    # 768-d multilingual embeddings
python-frontmatter==1.1.0
